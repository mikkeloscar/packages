[Unit]
Description=Continuous Integration platform 
After=network.target

[Service]
User=drone
Type=simple
EnvironmentFile=/etc/drone/dronerc
ExecStart=/usr/bin/droned
ExecReload=/usr/bin/kill -HUP $MAINPID
KillSignal=SIGINT
LimitNOFILE=8192
PrivateDevices=yes

[Install]
WantedBy=multi-user.target
